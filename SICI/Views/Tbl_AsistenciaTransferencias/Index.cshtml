@model IEnumerable<SICI.Modelo.Tbl_AsistenciaTransferencias>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}


<p>
    @Html.ActionLink("Nueva transferencia", "Create")
</p>
<table class="table">
    <tr>
        <th>
            Fecha registro
        </th>
        <th>
            Nombre completo
        </th>
        <th>
            Sede
        </th>
        <th>
            Transferencia
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.FechaRegistro)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Tbl_InformacionBasicaUsuario.NombreCompleto)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Tbl_Sedes.NombreSede)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Tbl_Transferencias.NombreTransferencia)
        </td>
        <td>
            <a href="@Url.Action("Edit", new { id=item.IdAsistenciaTransferencia })" class="btn btn-mini btn-info icon-Edit glyphicon glyphicon-pencil"> Editar</a>
            <a href="@Url.Action("Details", new { id=item.IdAsistenciaTransferencia })" class="btn btn-mini btn-success icon-Edit glyphicon glyphicon-eye-open"> Detalles</a>
            <a href="@Url.Action("Delete", new { id=item.IdAsistenciaTransferencia})" class="btn btn-mini btn-danger icon-Edit glyphicon glyphicon-trash"> Eliminar</a>
        </td>
    </tr>
}

</table>
