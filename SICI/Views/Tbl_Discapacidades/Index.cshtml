@model IEnumerable<SICI.Modelo.Tbl_Discapacidades>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Nueva discapacidad", "Create")
</p>
<table class="table">
    <tr>
        <th>
            Descripcion
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Descripcion)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdDiscapacidad }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdDiscapacidad }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdDiscapacidad })
        </td>
    </tr>
}

</table>
